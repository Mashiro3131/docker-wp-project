# # Removed the user directive
# worker_processes auto;  # Nombre de processus de travail automatique
# pid /run/nginx.pid;  # Emplacement du fichier PID

# events {
#     worker_connections 1024;  # Nombre maximum de connexions simultanées
# }

# http {
#     include /etc/nginx/mime.types;  # Inclure les types MIME
#     default_type application/octet-stream;  # Type MIME par défaut

#     # Paramètres de journalisation
#     access_log /var/log/nginx/access.log;  # Journal d'accès
#     error_log /var/log/nginx/error.log warn;  # Journal d'erreurs

#     # Compression Gzip pour les performances
#     gzip on;  # Activation de la compression Gzip
#     gzip_types text/css application/javascript application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript;  # Types de fichiers à compresser

#     # Optimisations d'envoi de fichiers et TCP
#     sendfile on;  # Activation de l'envoi de fichiers
#     tcp_nopush on;  # Activation de l'envoi immédiat des données
#     tcp_nodelay on;  # Activation de la désactivation de la mise en mémoire tampon
#     keepalive_timeout 65;  # Temps d'attente avant de fermer une connexion inactive
#     types_hash_max_size 2048;  # Taille maximale du cache des types MIME

#     server {
#         listen 80;  # Port d'écoute du serveur
#         server_name localhost;  # Nom du serveur

#         # Répertoire racine où NGINX sert les fichiers
#         root /usr/share/nginx/html;  # Répertoire racine

#         index index.php index.html index.htm;  # Fichiers index

#         # Bloc de localisation principal pour la gestion des fichiers statiques et du routage
#         location / {
#             try_files $uri $uri/ /index.php?$args;  # Tentative de trouver le fichier demandé
#         }

#         # Gestionnaire PHP
#         location ~ \.php$ {
#             include fastcgi_params;  # Inclusion des paramètres FastCGI
#             fastcgi_pass wordpress:9000;  # Connexion au PHP-FPM du conteneur WordPress
#             fastcgi_index index.php;  # Fichier index PHP
#             fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;  # Paramètre du nom du script PHP
#         }

#         # Interdiction d'accès aux fichiers cachés (.htaccess, etc.)
#         location ~ /\.(?!well-known).* {
#             deny all;  # Interdiction d'accès
#         }

#         # En-têtes de sécurité
#         add_header X-Content-Type-Options nosniff;  # Options de type de contenu
#         add_header X-Frame-Options SAMEORIGIN;  # Options de frame
#         add_header X-XSS-Protection "1; mode=block";  # Protection contre les attaques XSS
#         add_header Referrer-Policy no-referrer-when-downgrade;  # Politique de référence
#         add_header Content-Security-Policy "default-src 'self'; script-src 'self'; object-src 'none';";  # Politique de sécurité du contenu

#         # Contrôle du cache pour les fichiers statiques
#         location ~* \.(jpg|jpeg|gif|png|css|js|ico|woff|woff2|ttf|svg|eot)$ {
#             access_log off;  # Désactivation de la journalisation
#             expires 30d;  # Durée de validité du cache
#             add_header Cache-Control "public";  # Contrôle du cache
#         }

#         # Optionnel : Autoriser la récupération de favicon.ico et robots.txt sans journalisation
#         location = /favicon.ico { log_not_found off; access_log off; }
#         location = /robots.txt  { log_not_found off; }

#         # Restreindre l'accès aux fichiers et répertoires sensibles de WordPress
#         location ~* /(?:uploads|files)/.*\.php$ {
#             deny all;  # Interdiction d'accès
#         }

#         # Limiter la taille des fichiers téléchargés
#         client_max_body_size 10M;  # Taille maximale autorisée pour les fichiers téléchargés
#     }
# }

# /config/nginx/nginx.conf

server {
    listen 80;
    server_name localhost;

    root /var/www/html;
    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass wordpress:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    location ~ /\.ht {
        deny all;
    }
}
